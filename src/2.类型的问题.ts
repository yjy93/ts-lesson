// 什么时候标识类型, 什么时候不用 标识类型??

// ts 自带类型推导的功能

let name; // 当没有赋值的时候, 默认是 any

// 默认在初始化的时候,会进行类型推导.
let name1 = 'Gene'; // 自动推导出 name 为 string 类型.

// -----------------------------------------------

// number Number string String 之间的区别?


export {}
